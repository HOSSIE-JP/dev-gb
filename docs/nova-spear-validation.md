# NOVA SPEAR 検証記録

2026-09-08。既存のCaravan Editorで編集・ビルド・プレイできる、新作GB/GB Color縦STGの検証です。検証対象は `projects/nova-spear/assets-src/game.json` と元PNG、共通エンジンです。

## v7：時間制限OFF・ボス撃破演出・中盤の再調査（2026-09-08）

- NOVA SPEARの制限時間を無効化し、TIME表示を撤去。HUDは得点と残機だけ。旧作品の未指定設定は制限時間ありを維持。敵配置用の内部時刻は保持し、16bitの上限で飽和させて長時間プレイでイベントが再発しないようにした。
- ボス撃破後は敵・弾を整理し、機体周囲の12地点に繰り返し爆発を出す。64描画目で本体が消え、144描画以上の演出と新曲「Victory Flare」の終止を待ってから既存のステージフェードへ進む。最終ボスにも同じ演出を適用。演出中は通常ロジックを止め、得点や敵生成漏れの診断値に演出用オブジェクトが影響しない。
- 中盤の負荷は描画・敵更新が中心。波状／往復移動の事前計算テーブルを試したが、同条件の平均更新間隔に実用的な改善がなく不採用。描画待ちと手書きSM83 OAMループは維持。[測定条件・結果](nova-spear-performance.md)を参照。
- 型チェックと50件の回帰テスト成功。短縮した全3面・全ボスフェーズのDMG/CGB実ROMで、旧終了時刻を越える継続、複数位置の爆発、ボス本体消失、ファンファーレIDと非ゼロPCM、フェード後の次ステージ／最終クリアを確認。通常プレイの実OAMとアセット再構成の比較、SRAM保存・再起動・破損復旧も成功。
- NOVA SPEARとSTAR CARAVANのDebug／Releaseビルド成功。Release ROM: 131072 bytes、静的WRAM＋shadow OAM 1962 bytes、SHA-256 `9bc31873d687ab29f6a074cb67c2764f97e2579e56ec67006a69eb0801c99eaf`。
- Windowsラッパー、Windows Electron、BGB／Emulicious GUI、実機・聴感評価は環境不足で未実施。更新ROMの無改造全編クリアは未実施。エディターのロジックプレビューは撃破後の論理状態へ直進し、演出の確認にはビルドしたROMプレイを使用する。以下は旧版の履歴。

## v6：描画転送の待機とSM83アセンブラ（2026-09-08）

- 完成したshadow OAMをVBlankで実OAMへ転送してから次のゲーム更新へ進む。背景SCYも転送後のVBlankで確定。高負荷時は前の画面を保持して進行を遅くし、追いつき用のゲーム更新を行わない。
- 内蔵プレイヤーの通常速度も最大1エミュレート表示フレーム／描画に制限。Chromiumで100 msの遅延を5回挿入し、まとめ実行が発生しないことを確認。
- OAM組立ての内側を手書きSM83アセンブラ化。同じ待機方式のC版との比較で平均更新間隔はDMG約1.9％、CGB約3.0％短縮。v5より論理進行が遅くなる場面は意図した動作。
- 既存48件の全テストと、新しい遅延コールバックのテストが成功（49件）。更新後の型チェック成功。実OAM・shadow OAM・アセットから再構成したタイル列を比較し、全ボスフェーズの短縮DMG/CGB検証も成功。
- NOVA SPEARとSTAR CARAVANのDebug／Releaseビルド成功。SRAMの得点保存・ページ再読込後のスコア表示もChromiumで再確認。
- Release ROM: 131072 bytes、静的WRAM＋shadow OAM 1956 bytes、SHA-256 `287292bba30b7149f487cc273a7c9a8dff1d89dee1e58d7ce2864dfc413876a6`。
- Windowsラッパー、Windows Electron、BGB／Emulicious GUI、実機は未検証。更新ROMの無改造全編クリアは未実施。以下は旧版の履歴。

## v5：1.5秒復帰・追加最適化・SRAM・命中音（2026-09-08）

- 復帰待ちは90ゲーム更新（60Hz基準で約1.5秒）。処理落ち中は実時間が延びる。
- 処理別のサンプリング計測を実施。次回発射時刻のキャッシュと描画用作業変数の整理を採用し、座標シフトの試行は不採用。同条件の平均更新間隔はDMG約2.5％、CGB約1.4％短縮。ピークの処理落ちは残る。[性能記録](nova-spear-performance.md)を参照。
- 32行単位のループマップで全画面再転送・LCD停止を避ける経路を追加。実ROMの短縮マップで境界越えを確認。
- 上位5件の[SRAM保存](caravan-sram.md)を追加。MBC5＋8KiB RAM＋バッテリー。再起動、20段階の途中書込み、破損、世代周回、異なる作品のデータをDMG/CGBで検査。
- ボスの非致死ヒットで専用ノイズ音を再生し、実ROMの全ボスフェーズ検査で音源レジスタを確認。BGMのチャンネルは変更しない。
- 型チェックと全48件のテストが成功。NOVA SPEAR／STAR CARAVANのDebug・Releaseビルド成功。Chromiumで得点獲得→保存→ページ再読込→1500点のランキング復元を実操作・画像で確認。
- NOVA SPEAR Release: 131,072 bytes、静的WRAM＋shadow OAM 1,958 bytes、SHA-256 `3e26ba74eeca322a71dd2c6115a5ed0a9b4729ca08b6b8e8123c855bf6553545`。
- Windowsコマンドラッパー、Windows Electron、BGB/Emulicious GUI、実機は未検証。更新した無改造ROMの全編クリアは未実施。以下は旧版の検証履歴。

## v4：生成負荷・復帰待ち・場面転換（2026-09-08）

- 固定配列の再利用を維持し、空き枠ビットマップ・種別/OAM使用数のキャッシュ・弾速の事前計算・狙い撃ちの角度計算を改善。生成だけでなく衝突・爆発・描画が重なる負荷を測定。詳細と測定上の限界は[性能記録](nova-spear-performance.md)に記載。
- NOVA SPEARは被弾後180ゲーム更新の復帰待ちを追加。自弾を消し、復帰まで操作・射撃・再被弾を停止。敵・カメラ・制限時間は進行。復帰時に無敵時間を開始。処理落ち中は実時間が延びる。
- ステージのフェードアウト・インを既定で有効化。DMG/CGBの全3面短縮検証で5段階のパレット遷移と、暗転中にLCDを止めないことを確認。
- タイトルの `3 STAGES / 5 LIVES` を削除し、CGBタイトル画像を目視確認。
- 型チェック成功。47件の回帰テストを確認（復帰確認のカメラ参照を修正後、該当するDMG/CGB 2件を再実行して成功）。従来16方向と513×513入力で角度一致、自機不在中のゲーム進行、旧プロジェクトの即時復帰、ROM/シミュレーターの全ボスフェーズ一致を含む。
- NOVA SPEARとSTAR CARAVANのDebug/Releaseは成功。Release ROMは131,072 bytes、静的WRAM＋shadow OAMは1,665 bytes。SHA-256: `4afc2f9dbcbe6f9d21fad4ab71de3cafd7d0cb3936283370f000275a53e7522d`。
- Windowsラッパー・BGB/Emulicious GUI・実機は未検証。この更新ROMの無改造全編クリアは未実施。旧版の全編記録とは区別する。

## 1行HUD・性能改善（2026-09-08）

現在の版はHUDを1行にし、CPU・描画・内蔵エミュレーターの処理を改善しています。[変更内容・性能測定・制約](nova-spear-performance.md)を参照してください。以下は各過去版の検証記録です。

## 背景方向・テンポ改善（2026-09-08）

- 全3面を上から下へ流れる背景へ変更。`scrollDown`をステージ設定に追加し、未指定の従来作品は従来方向を維持。
- 実ROMとシミュレーターでカメラの開始位置・減算・ループ・非ループ終端を合わせ、下方向では新しく入る背景行だけ更新。
- HP1、射撃なし、80点のSPARKを50／70／70機追加。左右の縦列を90フレームずらし、ボス前の間を確保。得点上限は62,180点。
- 型チェック・全43件の回帰テスト成功。更新版の全ボスフェーズは短縮した検証用ステージでDMG/CGBの一致を確認。
- 無操作移動＋A連射のシミュレーションでは敵生成漏れ0。今回の更新版について、無改造ROMの全編クリアおよび実機確認は未実施。
- 以下のROMハッシュと全編クリア得点は初版の記録であり、更新版とは区別する。

## 初版のビルドと自動検証

| 項目 | 結果 |
| --- | --- |
| TypeScript | `npm run typecheck` 成功 |
| 回帰テスト | `npm run check`：41件成功、失敗0 |
| NOVA SPEAR Debug / Release | 実GBDK 4.5.0で双方65,536 bytes、MBC5、DMG/CGB共通 |
| RAM | 静的WRAM＋shadow OAM 1,501 bytes。別途スタック予算を確保 |
| スプライト | 97 / 128タイル |
| ゲームデータ・素材診断 | エラー0・警告0 |
| Release SHA-256 | `28dab0cc7f1db52f460cd3bf82532ca3df7091530f9df2efef60453f2ab018ea` |

テストには従来作品の回帰検証、自機A/B互換性、集中武器切替、ボス撃破必須の時間満了／終了イベント、DMG階調、BGM各曲のループ・終止・ポーズ・効果音チャンネル保持を含みます。実際のROMから音声PCMも確認しました。

実ROMのタイトル、開始、A広範囲射撃、B集中射撃、ポーズ、敵撃破点、ゲームオーバー、上位スコア画面をDMG/CGB両モードで確認しました。検証用コピーでボス出現・フェーズ条件を短縮し、全3ステージ・11フェーズのCランタイムの状態／エンティティ位置とシミュレーターの一致を確認しています。この短縮コピーは本番作品と分離されています。

## 本番ROMの通しプレイ

本番Release ROMのゲーム定義・HP・残機・ステージ時刻を一切変更せず、DMG/CGBそれぞれで全3ステージ・全11ボスフェーズを経てクリアしました。自動操縦は読み取り専用で状態を観測し、通常の十字キーとA/B入力だけを送ります。RAMへの書き込みや無敵化は行っていません。

| モード | 最終得点 | 残機 | ゲーム更新tick |
| --- | ---: | ---: | ---: |
| DMG | 46,620 | 4 | 11,907 |
| CGB | 46,520 | 4 | 11,909 |

別途、無改造のゲーム定義を使ったシミュレーションでも入力の記録・再生を行い、46,520点・残機5のクリアと最終状態の一致を確認しました。ハードウェアモードで描画や入力のサンプリング間隔が異なるため、実ROMの自動操縦経路と得点は完全一致を目的としていません。

## 再現手順

Windowsの固定環境ではリポジトリのルートから実行します。

```bat
bootstrap.cmd
doctor.cmd
build.cmd nova-spear -Configuration Debug
build.cmd nova-spear -Configuration Release
test.cmd
editor.cmd nova-spear
```

`editor`内の`npm run check`で利用可能な全回帰テストを実行できます。GBDKのないホストではROM検証がスキップされるため、`npm run test:rom`で前提条件不足をエラーにして実行してください。CIも`test.cmd`とReleaseビルドを実行し、`nova-spear-game-boy`にプレイ用ROMを保存します。

## 確認範囲

今回のローカルROM実行はBoytaceanのDMG/CGBエミュレーションです。BGB／EmuliciousのGUI、実機カートリッジでの動作と聴感確認はこの実行環境では未実施です。ゲーム内の秒数は60Hzの論理更新に基づき、負荷時の処理落ちは残ります。スコアは起動中のRAM内のみで電源断後は保持しません。

ソースと検証作業用コピーのSHA-256を照合してから検証しました。共有作業ディレクトリでのビルドロック干渉を避けるため、ROM検証は独立した一時コピーで実施しています。生成ROM、スクリーンショット、録画はGitの追跡対象に含めません。

## エディター操作

production版のReact画面をブラウザーで操作し、実際のプロジェクト保存・GBDKコンパイラー・WASM ROM再生へ接続して確認しました。NOVA SPEARの作業用複製で、タイトル編集、Undo/Redo、保存、全素材検索、コマンドパレット、タイル選択・描画、F5相当のビルドしてプレイ、ポーズ／ステップ、再プレイ、別作品への切替と再ビルドが成功しています。ページエラー0件。OS固有のElectronウィンドウ／ファイルダイアログの検証とは分けて記録しています。

160×144背景の変換は、png2assetの中間マップが256タイルを超えないよう完全なタイル行で分割しています。結合後は元画像の2bppデータとの完全一致を検査します。最終変換処理でNOVA SPEAR ReleaseとSTAR CARAVAN Debugを再ビルドし、変換ツールの警告0件、修正前後のROMバイト列一致を確認しました。
