# v0.13 エンディング差分

霊夢の既存最終画面と同じ、お茶を持ったバストアップで魔理沙の一枚を追加しました。ボスだけが描かれた既存の前5枚は共用し、最後の一枚を選択機体で切り替えます。既存原画は再生成していません。

原画、生成指示、参照画像のハッシュはこのフォルダに保存しています。convert.cjs が最近傍・4階調・タイル数検査を行い、144×128を160×144中央に置いた238タイルの画像に変換します。採用パレットは7。assets-src/images/ending-marisa.png とエディター内の同名画像が実装の編集元です。

検証: リポジトリのNodeで convert.cjs --verify。再採用はエディターの readGame/saveGame を経由します。生成Cを直接編集しません。
